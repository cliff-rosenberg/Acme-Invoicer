{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h3>Current User:</h3>
    <p>{{user.username}}</p>
  </div>
</div>
{{/each}}

<nav class="nav flex-column">
  <a class="nav-link active" href="#">Home</a>
  <a class="nav-link" href="#">Show Customers</a>
  <a class="nav-link" href="#">Show Inventory</a>
  <a class="nav-link" href="#">Show Invoices</a>
</nav>
