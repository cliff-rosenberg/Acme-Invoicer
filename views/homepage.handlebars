{{#each users as |user| }}
<div class="flex-column mb-5">
  <div class="col-md-6">
    <h3>Current User:</h3>
    <p>{{user.username}}</p>
  </div>
</div>
{{/each}}

<img src="" class="img-fluid" alt="Acme Invoicer Logo">