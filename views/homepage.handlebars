{{#each users as |user| }}
<div class="flex-column mb-5">
  <div class="col-md-6">
    <h3>Current User:</h3>
    <p>{{user.username}}</p>
  </div>
</div>
{{/each}}

<div class="text-center">
<img src="/assets/grayscale_transparent_sm.png" class="img-fluid" alt="Acme Invoicer Logo">
</div>
